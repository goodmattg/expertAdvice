def showAgainstPredict(dat, year, quarter):
        # for q in years.groups
    yfocus = dat[dat['YEAR']==2011]
    qfocus = yfocus[yfocus['QUARTER']==1]
    mp.hist(qfocus['RGDP'])

def getAdvice(frame, qstart, ystart, qend, yend):

def getOutcomes(frame, qstart, ystart, qend, yend):

